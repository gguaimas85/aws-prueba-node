const fs = require("node:fs");

let constantes = {
  CONTENT_TYPE: "Content-Type",
  TYPE_HTML: "text/html",
  TYPE_JSON: "application/json",
  HOSTNAME: "127.0.0.1",
  PORT: 3000,
};

module.exports.constantes = constantes;
